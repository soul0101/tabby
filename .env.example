# Copy to .env.local and fill in from `npx supabase start`, which prints these
# when the local stack comes up.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Only needed to push migrations or config at a hosted project.
# SUPABASE_DB_PASSWORD=
